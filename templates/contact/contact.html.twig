{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block stylesheets %}
    <script src='https://www.google.com/recaptcha/api.js'></script>
{% endblock %}

{% block body %}
    <h1 class="mb-4">Contact</h1>
    <p>Vous avez une question, une remarque, ou simplement un message quelconque à nous transmettre ? N'hésitez pas à
        nous contacter en utilisant le formulaire ci-dessous !</p>
    <p>Votre message sera enregistré et transmis au personnel du CIS Saint Julien de Concelles. Vos coordonnées nous
        serviront uniquement à vous recontacter si nécessaire.</p>
    <p>Nous vous rappellons que toutes les données envoyées resteront internes au CIS et ne seront transmises à
        personne.</p>
    <h4 class="my-4">Formulaire de contact</h4>
    {{ form_start(form) }}
        <div class="form-row">
            <div class="form-group col">
                {{ form_row(form.prenom) }}
            </div>
            <div class="form-group col">
                {{ form_row(form.nom) }}
            </div>
        </div>
        <div class="form-row">
            <div class="form-group col">
                {{ form_row(form.email) }}
            </div>
            <div class="form-group col">
                {{ form_row(form.phone) }}
                <small class="small text-muted">Le numéro doit commencer par 0 ou +33 et ne doit pas comporter d'espaces (ex&nbsp;:&nbsp;0607080910)</small>
            </div>
        </div>
        <div class="form-row">
            <div class="form-group col">
                {{ form_row(form.object) }}
            </div>
        </div>
        <div class="form-row">
            <div class="form-group col">
                {{ form_row(form.message) }}
            </div>
        </div>

        <div class="row">
            <div class="d-block m-auto">
                <div class="g-recaptcha" data-sitekey="6LevW1gUAAAAALUekVHYI7TcmtxqbOoFNs9cnYR3"></div>
            </div>
        </div>
        <input type="submit" value="Envoyer le message" class="btn btn-danger d-block mx-auto mt-4">
    {{ form_end(form) }}
{% endblock %}
